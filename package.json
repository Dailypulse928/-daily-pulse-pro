{
  "name": "daily-pulse-functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Daily Pulse Pro",
  "engines": { "node": "24" },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
